# 数据保护规则（不可协商）

## 铁律

**永远不删除报告数据。** 任何情况下都不删。

这条规则由小仙女于 2026-08-20 明确要求，适用于本项目的所有维护者，
包括 AI 助手。

## 具体含义

以下目录及其内容**只能新增、只能读取，绝不删除**：

```
$DATA_DIR/
├── users.json          账号
├── codes.json          查询码映射
└── sessions/<id>/      画作、问卷、报告
```

## 禁止的操作

- ❌ `rm -rf $DATA_DIR` 或其任何子目录
- ❌ 从 `codes.json` 中移除条目
- ❌ 从 `users.json` 中移除用户
- ❌ 任何形式的"清理测试数据"脚本
- ❌ 部署、重构、迁移时"顺手清空"

## 允许的替代方案

需要让某条数据不再出现时，**改代码逻辑，不动数据**：

- 加 `hidden: true` 标记，查询时过滤
- 加 `archivedAt` 时间戳，列表默认不展示
- 加 `isTestData: true`，生产视图排除

数据留在磁盘上，只是不显示。这样任何误判都可以撤销。

## 历史教训

2026-08-20，我（大狗狗）在部署过程中为避免把隐私数据提交到 git，
多次执行 `rm -rf data`，**删掉了小仙女之前生成的报告**。
她后来问起时才发现。

同日晚些时候，我又写脚本删除了自己创建的测试账号及其会话
（`__test__` / `__kbtest__`）。虽然只删了自己造的数据、
她和 donghl 的账号与报告完好，但这个习惯本身就是危险的 ——
脚本只要多匹配一个 ownerId，删掉的就是真实用户的东西。

**结论：不要养成删数据的习惯，哪怕"只是测试数据"。**
测试数据应该通过标记隐藏，或者用独立的 `DATA_DIR` 完全隔离。

## 测试时的正确做法

```bash
# ✅ 用临时目录，完全不碰生产数据
DATA_DIR=/tmp/pp-test npm run dev

# ❌ 不要在生产数据目录里测试再清理
```
